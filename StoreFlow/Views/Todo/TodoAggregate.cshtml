@model List<Todo>

@{
    Layout = "_Layout";
    ViewBag.Title = "Yapılacaklar Listesi";
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                @ViewBag.Result
            </div>
        </div>
    </div>
</div>